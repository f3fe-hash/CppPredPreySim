#pragma once

#include <vector>

using num     = float;
using num_arr = std::vector<num>;

using score = double;

template<typename T>
using vec = std::vector<T>;

constexpr const num zero = (num)0.00;